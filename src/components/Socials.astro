---
import { getCollection } from "astro:content";
import Social from "./Social.astro";

const socials = await getCollection("socials");
---

<div class="socials">
  {socials.map((social) => <Social {...social.data} />)}
</div>

<style>
  .socials {
    display: flex;
  }
</style>
